import React from 'react';


//import Counter from './Counter';

const Card=({value})=>{
    return (
        <div className="card-container">
            <p>{value}</p>

    
        </div>
    );
};

export default Card;